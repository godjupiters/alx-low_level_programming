#include "main.h"
/**
 * isdigit - scans for digits from 0 to 9
 * @c: variable to be checked
 * Return: returns 1 when variable is digit or 0 when otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9');
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
