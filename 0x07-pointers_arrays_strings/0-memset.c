#include "main.h"

/**
 * _memset - a prototype function that fills memory with a constant byte
 * The _memset() function fills the first n bytes of
 * the memory area pointed to by s with the constant byte b
 * Returns a pointer to the memory area s
 *
 * @s: the pointer address
 * @b: the variable value
 * @n: variable for the number of bytes
 *
 * Return: s pointer address
 */

char *_memset(char *s, char b, unsigned int n)
{
	int a = 0;

	for (n > 0; i++)
	{
		s[a] = b;
		n--;
	}
	{
	return (s);
	}
}
